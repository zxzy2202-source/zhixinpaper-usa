# Zhixin Paper 网站全面检查与修复状态报告

检查日期：2026-08-02
最新复核：2026-08-02
检查范围：`E:\website\zhixinpaper-usa-main` 最新源码、本地生产预览 `http://127.0.0.1:3031`、本地 sitemap 与路由契约。
操作边界：未提交真实表单，未写入生产数据，未提交或推送代码，未部署，未写生产后台优化日志。

## 结论摘要

- **原 P1 问题 5 类均已完成源码修复**：询盘成功契约、隐私与 Cookie 披露、证书申请契约、案例单一数据源、ESLint 工程门禁。
- **高风险公开声明已集中收敛**：删除或限定无证据的证书、法规、工厂设备与产能、客户与市场覆盖、产品性能与兼容性、MOQ、样品、响应时间、交期、配送和 DDP 声明。
- **工程质量门禁通过**：TypeScript、ESLint、28 项测试、生产构建均通过；Next.js 生成 117 个静态页面。
- **SEO 与路由契约通过**：sitemap 共 97 个 URL，非 200、缺 title、缺 canonical、canonical 路径不一致均为 0；6 类无效路由均返回真实 404。
- **重点页面响应式验收通过**：七个重点页面在 1440×900 与 390×844 共 14 个页面/视口组合中均正常渲染；未发现页面横向溢出、真实文字裁切、破图、不安全宽表格或错误级控制台消息，Virtual Tour 视频在两个视口均加载有效元数据。
- **尚未部署**：本报告只确认本地源码和本地生产构建，不能替代公开站点发布后的复核。

## 已修复：P1 高优先级问题

### P1-1 询盘接口假成功

**状态：已修复并通过测试。**

Contact、Quote、Samples 接口现在分别记录数据库落库结果和销售通知结果。只有数据库落库成功或至少一个销售通知通道成功时才确认询盘已接收；两者都失败时返回 503。自动回复状态通过 `autoReplySent` 独立表达，不再与询盘接收状态混用。

验证证据：Inquiry Delivery 5/5 测试通过，覆盖双通道失败、仅数据库成功、仅通知成功、双通道成功和自动回复独立状态。

### P1-2 隐私与 Cookie 披露不一致

**状态：已修复源码契约。**

Privacy Policy 已披露表单字段、IP 与技术请求信息、数据库、邮件与内部销售通知、OKKI、跨境处理、保留、安全措施和数据主体权利。Cookie Policy 已区分必要存储与可选 OKKI 分析。OKKI 脚本仅在访客选择 “Accept analytics” 后加载；选择 “Essential only” 时不加载。

限制：法律依据、跨境机制、各服务商精确保留期限仍需由业务和法律负责人结合真实供应商合同确认，源码不能替代法律审查。

### P1-3 证书中心下载承诺与实际交互不一致

**状态：已修复。**

证书中心已改为文档申请与项目审核中心，不再声称所有文件可直接下载，不再展示无公开证据支持的固定 PDF 文件大小或有效期。每类文件均要求按产品等级、完整结构、用途、目的市场、持证主体、签发方、有效期和适用范围确认。

### P1-4 案例聚合页与详情页数据冲突

**状态：已修复。**

案例聚合页和详情页现在共同使用 `src/lib/case-studies.ts` 单一数据源；聚合卡片链接对应详情页。案例改为匿名项目资格审查过程，删除未经证据支持的客户规模、国家覆盖、准时率、节省金额、失败率及保证性结果，并增加适用条件和验证说明。

### P1-5 ESLint 门禁失效

**状态：已修复。**

ESLint 9 flat config 已生效，显式忽略 `.next`、`node_modules`、`public`、`coverage` 和 `data` 等生成或非源码目录；lint 只检查 `src`、`tests` 和 `scripts`，并以零 warning 为门禁。`package.json` 已提供独立 `typecheck` 命令，README 已改为真实项目文档。

剩余维护项：`eslint-config-next` 仍为 15.3.1，而 Next.js 为 16.2.3。当前 lint 与构建已通过，但后续依赖维护应在确认 npm/pnpm 单一包管理策略后统一升级并更新对应锁文件。

## 已修复：公开声明与内容治理

### 合规与法规

- EU food-contact 页面改为按材料、完整结构、接触类型、食品类别、温度、时间、迁移条件、用途和目的地审核。
- REACH / RoHS 页面移除所有产品统一达标、固定阈值结论、年度筛查和完整供应链追溯等无证据声明。
- 美国 cannabis labels 页面不再发布固定州机构、追踪系统、符号、二维码、语言、有效期或包装要求，改为辖区、牌照、产品类别、生效日期和已批准稿件审核框架。
- 产品详情页中的证书、声明、TDS 和测试报告均限定为报价等级、当前版本、持证主体、签发方、有效期和适用范围审核。

### 工厂、客户与市场覆盖

- Virtual Tour 移除固定涂布线、分切线、印刷线、模切站、速度、仓储面积、每批检测和全球备货等无证据陈述。
- Header 移除“服务 13+ 行业全球客户”式覆盖声明。
- 行业结构化数据的服务区域收敛为站点明确面向的欧盟、英国、美国、加拿大和墨西哥，不再使用 `Worldwide`。

### 性能、兼容性与商业承诺

- 打印机兼容性改为按具体设备、固件、尺寸、卷向、感应、设置、条码、扫描器和实物样品验证。
- 温度、粘接、耐水、耐刮、耐候、成像稳定性等性能改为材料、表面、环境、测试条件和目标寿命相关的项目验证。
- MOQ、样品费用与可用性、准备时间、交期、运输时间和 DDP 均改为报价与书面确认事项，不再作无条件承诺。
- TITO 与 cannabis 博客移除固定速度、交易规模、平台要求和绝对性能结论。

目标风险词复核未发现原审计中的固定密钥、通用合规、固定设备产能、免费样品、固定响应或保证交付声明。搜索意图关键词和普通导航文案（例如 “All products”）不属于事实承诺，予以保留。

## 新增安全修复

### 生产会话密钥

**状态：已修复。**

生产环境不再回退到源码内置固定 JWT 签名密钥。部署时必须配置 `ADMIN_SESSION_SECRET` 或兼容的 `NEXTAUTH_SECRET`；只有非生产本地开发允许使用开发回退值。`.env.example` 已明确要求使用长随机值。

## P2 与剩余事项

### P2-1 动态博客 sitemap 降级可观测性

**状态：部分处理，仍待增强。**

数据库博客查询失败时 sitemap 会保留静态 URL 并记录 `console.error`，避免 sitemap 整体不可用，但后台文章数量异常仍缺少独立监控或健康检查。建议后续增加动态博客数量检查和告警，不应让发布文章静默退出 sitemap。

### P2-2 地区 Open Graph locale

**状态：已处理并通过运行态验证。**

加拿大主页与 cannabis 子页使用 `en_CA`，欧洲主页及英国页使用 `en_GB`；德国、法国、荷兰和波兰国家详情页分别使用 `en_DE`、`en_FR`、`en_NL` 和 `en_PL`。墨西哥当前保持英文页面，未来真实西班牙语内容仍应使用独立语言路径。生产构建后逐页读取实际 HTML，六个本次目标页面均返回 200 且 `og:locale` 与预期一致。

### P2-3 响应式与浏览器运行态

**状态：七个重点页面已通过独立 Chromium/CDP 验收。**

验收页面包括：

- `/compliance/eu-food-contact`
- `/compliance/reach-rohs`
- `/factory/virtual-tour`
- `/us/cannabis-labels`
- `/blog/cannabis-label-requirements-usa`
- `/products/thermal-paper-rolls/standard-pos-rolls`
- `/products/thermal-labels/freezer-cold-chain-labels`

以上页面分别在 1440×900 与 390×844 视口重新导航并完成只读 DOM 几何检查，共 14 个页面/视口组合，全部通过。检查覆盖非空 React 渲染、页面级横向溢出、文本行盒与 `overflow: hidden/clip` 祖先交集、图片自然尺寸、移动端宽表格滚动容器及错误级控制台消息。所有页面横向溢出均为 0，文字裁切、破图、不安全宽表格和错误级控制台消息均为 0。Virtual Tour 视频在两个视口均使用 `/videos/factory-tour.mp4`，`readyState=4`、`networkState=1`、无媒体错误，元数据为 1280×720、177.86 秒。

### P2-4 公开站点部署差异

**状态：待用户授权部署后核验。**

本轮没有发布代码。公开站点可能仍运行旧版本；本地通过不代表公开页面、表单通知、数据库、缓存、sitemap 或 `/mx` 已同步。部署后需重新检查公开 URL、真实表单通道、canonical、sitemap 和真实 404。

## 最新验证结果

- `npm run typecheck`：通过。
- `npm run lint`：通过，0 warning。
- `npm test`：28/28 通过。
  - Product Explorer：5/5。
  - Optimization Log：4/4。
  - Contracts：14/14。
  - Inquiry Delivery：5/5。
- `npm run build`：通过；Next.js 16.2.3 生产构建成功，生成 117 个静态页面。
- 本地 sitemap：97 个 URL。
  - 非 200：0。
  - 缺 title：0。
  - 缺 canonical：0。
  - canonical path 不一致：0。
- `robots.txt`：200，并声明生产 sitemap URL。
- 真实 404：随机根路由、无效 thermal paper、thermal label、blog、US 和 CA slug 共 6 项，全部返回 404，无重定向或软 404。
- `git diff --check`：无空白错误；仅有 Git 的 LF/CRLF 转换提示。
- 外部字体检查：未发现 Google Fonts 或外部 CSS `@import`。
- 本地生产预览：`http://127.0.0.1:3031` 返回 200。
- 重点页面响应式验收：7 个路由 × 2 个视口，共 14 组全部通过；横向溢出、真实文字裁切、破图、不安全宽表格和错误级控制台消息均为 0。
- Virtual Tour 视频：桌面与移动视口均从 `/videos/factory-tour.mp4` 完成媒体加载，`readyState=4`、`networkState=1`、无错误，视频元数据为 1280×720、177.86 秒。

## 检查限制

- 未提交 Contact、Quote 或 Samples 真实表单，避免产生询盘、邮件或通知；询盘结论来自控制流审计和 5 项契约测试。
- 未核验真实证书、测试报告、客户授权、设备清单、产能数据或法规批准；本轮策略是移除或限定无法由仓库证据支持的公开声明，而不是断言这些材料不存在。
- 本次响应式结论仅覆盖上述七个重点页面与两个指定视口，不等同于所有公开路由、浏览器内核和中间宽度均无视觉回归。
- 未部署、未写生产数据库、未写后台优化日志、未提交或推送代码。

## 后续执行顺序

1. 确认项目唯一包管理器后，将 `eslint-config-next` 与 Next.js 主版本对齐并复跑全部门禁。
2. 将 `/api/health/sitemap` 接入部署监控或告警，避免数据库发布文章静默退出 sitemap。
3. 经明确授权后提交、推送和部署，再对公开站点执行页面、表单通道、sitemap、canonical、404 和 `/mx` 回归。
